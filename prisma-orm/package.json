{
  "name": "prisma-orm",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/generated/prisma/client.js",
  "types": "dist/generated/prisma/client.d.ts",
  "scripts": {
    "migrate:dev": "npx prisma migrate dev",
    "migrate:prod": "npx prisma migrate deploy",
    "seed": "npx tsx --env-file=.env prisma/seed.ts",
    "start": "npx prisma generate && npx tsc --build && npm run migrate:prod && npm run seed",
    "studio": "npx prisma studio"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "prisma": "^6.16.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^6.16.1",
    "@prisma/client": "^6.16.1",
    "dotenv": "^17.2.2"
  }
}