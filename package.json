{
    "name": "ossi-api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "seed": "docker compose run -v ./prisma-orm/:/app --env-from-file=.env --rm db-migrations npm run seed",
        "start": "docker compose -f docker-compose.yml up -d --build",
        "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
        "studio": "docker compose run -v ./prisma-orm/:/app -p 5556:5555 --rm db-migrations npm run studio",
        "test": "docker compose -f docker-compose.test.yml up"
    },
    "workspaces": [
        "./prisma-orm",
        "./api-gateway",
        "./auth-api",
        "./messaging-server",
        "./notification-server",
        "./student-management-api"
    ]
}
